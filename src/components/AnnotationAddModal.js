import React from 'react';
import { Box } from '@codeday/topo/Atom'

export default function AnnotationAddModal({isOpen, onClose, onSubmit, x, y}) {
    return (
        <Box>
            Test
        </Box>
    )
}
